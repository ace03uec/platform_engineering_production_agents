apiVersion: 1
datasources:
  - name: Prometheus
    uid: workshop-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: 5s
